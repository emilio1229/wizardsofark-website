# Legacy Fastify + SQLite monitor
#
# This directory is superseded by `apps/api` (NestJS + Prisma + PostgreSQL).
# Kept temporarily for reference during the monorepo cutover; do not deploy.
#
# See README.md for the new architecture.
